#include "main.h"
#include <stdio.h>

/**
 * _strlen - returns the length of a string
 *
 * @s: string
 *
 * Return: string
 */

int _strlen(char *s)
{
	int i = 0;

	while (*str++)
	{
		i++;
	}
	return (i);
}
